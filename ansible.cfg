[defaults]
inventory= ./hosts
remote_user= ubuntu
# to ask password everytime or not
ask_pass= false
host_key_checking= false
[privilege_escalation]
become = true
become_user = root
become_method = sudo
become_ask_pass = false

[netconf_connection]
ssh_config = ./config
